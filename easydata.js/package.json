{
	"name": "EasyData.JS",
	"version": "1.0.0",
	"description": "",
	"scripts": {
		"installall": "cd packs/crud && npm install",
		"build:dev": "npm run linkall && cd packs/crud && npm run build:dev",
		"build:prod": "npm run linkall && cd packs/crud && npm run build:prod",
		"packall": "cd packs/crud && npm pack",
		"setver": "node ./tools/set_version.js",
		"linkall": "node ./tools/set_links.js",
		"test": "cd packs/crud"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"cpx": "^1.5.0"
	}
}
